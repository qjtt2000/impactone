# V3.17.3 Footer Final Alignment Fix

- Desktop footer gold slogan now starts on the same vertical line as “出品｜IMPACTONE”.
- Brand row uses content-width columns with space-between so the alignment rail is based on the brand text, not the logo image.
- Gold slogan span is slightly widened to match the approved reference, ending approximately under the “家” character in “五大道企业家基金会”.
- Mobile layout is unchanged.
- Existing V3.17 functions remain unchanged, including reader font-size switching, detail expand/collapse, issue navigation, subscription, share, favorites, comments, auth, PWA and backend integration.
- Service worker cache key updated so the latest CSS is fetched after deployment.
